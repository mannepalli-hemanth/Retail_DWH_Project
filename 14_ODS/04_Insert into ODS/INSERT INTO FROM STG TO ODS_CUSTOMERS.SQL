INSERT INTO ODS.ODS_CUSTOMERS
SELECT
    CUSTOMER_ID,
    FIRST_NAME,
    LAST_NAME,
    EMAIL,
    PHONE,
    GENDER,
    DATE_OF_BIRTH,
    CITY,
    STATE,
    COUNTRY,
    CUSTOMER_STATUS,
    CREATED_DATE,
    UPDATED_DATE,
    CURRENT_TIMESTAMP() AS CREATED_AT,
    CURRENT_TIMESTAMP() AS UPDATED_AT
FROM STAGING.STG_CUSTOMERS;

SELECT * FROM ODS.ODS_CUSTOMERS LIMIT 50; 