
INSERT INTO ODS.ODS_PRODUCTS(
    PRODUCT_ID,
    PRODUCT_NAME,
    CATEGORY,
    SUB_CATEGORY,
    BRAND,
    UNIT_PRICE,
    COST_PRICE,
    PRODUCT_STATUS,
    CREATED_DATE,
    UPDATED_DATE,
    CREATED_AT,
    UPDATED_AT
)
SELECT
    PRODUCT_ID,
    PRODUCT_NAME,
    CATEGORY,
    SUB_CATEGORY,
    BRAND,
    UNIT_PRICE,
    COST_PRICE,
    PRODUCT_STATUS,
    CREATED_DATE,
    UPDATED_DATE,
    CURRENT_TIMESTAMP() AS CREATED_AT,
    CURRENT_TIMESTAMP() AS UPDATED_AT
FROM STAGING.STG_PRODUCTS;

SELECT * FROM ODS.ODS_PRODUCTS LIMIT 50 ;