INSERT INTO ODS.ODS_PAYMENTS(
    PAYMENT_ID,
    ORDER_ID,
    PAYMENT_METHOD,
    PAYMENT_STATUS,
    PAYMENT_AMOUNT,
    PAYMENT_DATE,
    CREATED_AT,
    UPDATED_AT
)
SELECT
    PAYMENT_ID,
    ORDER_ID,
    PAYMENT_METHOD,
    PAYMENT_STATUS,
    PAYMENT_AMOUNT,
    PAYMENT_DATE,
    CURRENT_TIMESTAMP() AS CREATED_AT,
    CURRENT_TIMESTAMP() AS UPDATED_AT
FROM STAGING.STG_PAYMENTS;

SELECT COUNT(*) FROM STAGING.STG_PAYMENTS;
SELECT COUNT(*) FROM ODS.ODS_PAYMENTS;

