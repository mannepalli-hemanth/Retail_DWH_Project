INSERT INTO ODS.ODS_SHIPMENTS(
    SHIPMENT_ID,
    ORDER_ID,
    COURIER_NAME,
    TRACKING_NUMBER,
    SHIPMENT_STATUS,
    SHIPPED_DATE,
    DELIVERY_DATE,
    CREATED_AT,
    UPDATED_AT
)
SELECT
    SHIPMENT_ID,
    ORDER_ID,
    COURIER_NAME,
    TRACKING_NUMBER,
    SHIPMENT_STATUS,
    SHIPPED_DATE,
    DELIVERY_DATE,
    CURRENT_TIMESTAMP() AS CREATED_AT,
    CURRENT_TIMESTAMP() AS UPDATED_AT
FROM STAGING.STG_SHIPMENTS;

SELECT COUNT(*) FROM STAGING.STG_SHIPMENTS;
SELECT COUNT(*) FROM ODS.ODS_SHIPMENTS;

