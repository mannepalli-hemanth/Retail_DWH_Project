INSERT INTO ODS.ODS_ORDER_ITEMS(
    ORDER_ITEM_ID,
    ORDER_ID,
    PRODUCT_ID,
    QUANTITY,
    UNIT_PRICE,
    DISCOUNT,
    CREATED_DATE,
    CREATED_AT,
    UPDATED_AT
)
SELECT
    ORDER_ITEM_ID,
    ORDER_ID,
    PRODUCT_ID,
    QUANTITY,
    UNIT_PRICE,
    DISCOUNT,
    CREATED_DATE,
    CURRENT_TIMESTAMP() AS CREATED_AT,
    CURRENT_TIMESTAMP() AS UPDATED_AT
FROM STAGING.STG_ORDER_ITEMS;

SELECT COUNT(*) FROM STAGING.STG_ORDER_ITEMS;
SELECT COUNT(*) FROM ODS.ODS_ORDER_ITEMS;
