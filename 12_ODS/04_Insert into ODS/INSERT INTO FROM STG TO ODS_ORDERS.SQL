INSERT INTO ODS.ODS_ORDERS(
    ORDER_ID,
    CUSTOMER_ID,
    ORDER_DATE,
    ORDER_STATUS,
    TOTAL_AMOUNT,
    STORE_ID,
    CREATED_DATE,
    UPDATED_DATE,
    CREATED_AT,
    UPDATED_AT
)
SELECT
    ORDER_ID,
    CUSTOMER_ID,
    ORDER_DATE,
    ORDER_STATUS,
    TOTAL_AMOUNT,
    STORE_ID,
    CREATED_DATE,
    UPDATED_DATE,
    CURRENT_TIMESTAMP() AS CREATED_AT,
    CURRENT_TIMESTAMP() AS UPDATED_AT
FROM STAGING.STG_ORDERS;

SELECT COUNT(*) FROM STAGING.STG_ORDERS;
SELECT COUNT(*) FROM ODS.ODS_ORDERS;