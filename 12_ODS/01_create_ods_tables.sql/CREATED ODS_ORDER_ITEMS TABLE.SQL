CREATE OR REPLACE TABLE ODS.ODS_ORDER_ITEMS(
    ORDER_ITEM_ID NUMBER,
    ORDER_ID NUMBER,
    PRODUCT_ID NUMBER,
    QUANTITY NUMBER,
    UNIT_PRICE NUMBER,
    DISCOUNT NUMBER,
    CREATED_DATE TIMESTAMP,
    CREATED_AT TIMESTAMP,
    UPDATED_AT TIMESTAMP
);